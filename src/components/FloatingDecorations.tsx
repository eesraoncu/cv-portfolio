import React from 'react';
import { motion } from 'framer-motion';

// FloatingDecorations is no longer needed with the new clean design.
// Kept as a no-op component for backward compatibility.
const FloatingDecorations: React.FC = () => null;

export default FloatingDecorations;
